AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: CloudVault AI Backend Infrastructure

Globals:
  Function:
    Timeout: 10
    MemorySize: 256
    Runtime: nodejs20.x
    Architectures:
      - x86_64
    Environment:
      Variables:
        UPLOAD_BUCKET: !Ref StorageBucket
        FILE_TABLE: !Ref FileMetadataTable

Resources:
  # ─── Cognito User Pool (Phase 6) ───────────────────────────────────────────
  CloudVaultUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: CloudVaultUserPool
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: false

  # Cognito User Pool Client — no secret (required for browser JS clients)
  CloudVaultUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      UserPoolId: !Ref CloudVaultUserPool
      ClientName: CloudVaultWebClient
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_USER_PASSWORD_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH
      PreventUserExistenceErrors: ENABLED

  # ─── HTTP API with CORS + Cognito JWT Authorizer ────────────────────────────
  CloudVaultApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      Auth:
        DefaultAuthorizer: CognitoAuthorizer
        Authorizers:
          CognitoAuthorizer:
            IdentitySource: "$request.header.Authorization"
            JwtConfiguration:
              issuer: !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/${CloudVaultUserPool}"
              audience:
                - !Ref CloudVaultUserPoolClient
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS
        AllowHeaders:
          - Content-Type
          - Authorization

  # ─── Storage Bucket ─────────────────────────────────────────────────────────
  StorageBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "cloudvault-ai-storage-${AWS::AccountId}-${AWS::Region}"
      CorsConfiguration:
        CorsRules:
          - AllowedHeaders:
              - "*"
            AllowedMethods:
              - PUT
              - GET
              - POST
            AllowedOrigins:
              - "*"
            ExposedHeaders:
              - ETag
            MaxAge: 3000

  # ─── DynamoDB Table ─────────────────────────────────────────────────────────
  FileMetadataTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: "CloudVaultFiles"
      AttributeDefinitions:
        - AttributeName: "userId"
          AttributeType: "S"
        - AttributeName: "fileId"
          AttributeType: "S"
      KeySchema:
        - AttributeName: "userId"
          KeyType: "HASH"
        - AttributeName: "fileId"
          KeyType: "RANGE"
      BillingMode: PAY_PER_REQUEST

  # ─── Lambda: Get Upload URL (Phase 1) ───────────────────────────────────────
  GetUploadUrlFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: functions/getUploadUrl.handler
      Policies:
        - S3WritePolicy:
            BucketName: !Ref StorageBucket
      Events:
        ApiEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref CloudVaultApi
            Path: /files/upload-url
            Method: POST

  # ─── Lambda: Confirm Upload (Phase 3) ───────────────────────────────────────
  ConfirmUploadFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: functions/confirmUpload.handler
      Policies:
        - DynamoDBWritePolicy:
            TableName: !Ref FileMetadataTable
      Events:
        ApiEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref CloudVaultApi
            Path: /files/confirm
            Method: POST

  # ─── Lambda: List Files (Phase 3) ───────────────────────────────────────────
  ListFilesFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: functions/listFiles.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref FileMetadataTable
      Events:
        ApiEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref CloudVaultApi
            Path: /files
            Method: GET

  # ─── Lambda: Delete File (Phase 4) ──────────────────────────────────────────
  DeleteFileFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: functions/deleteFile.handler
      Policies:
        - DynamoDBWritePolicy:
            TableName: !Ref FileMetadataTable
      Events:
        ApiEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref CloudVaultApi
            Path: /files/{fileId}
            Method: DELETE

  # ─── Lambda: Get File URL (Phase 4) ─────────────────────────────────────────
  GetFileUrlFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/
      Handler: functions/getFileUrl.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref FileMetadataTable
        - S3ReadPolicy:
            BucketName: !Ref StorageBucket
      Events:
        ApiEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref CloudVaultApi
            Path: /files/{fileId}/url
            Method: GET

Outputs:
  ApiEndpoint:
    Description: "API Gateway endpoint URL"
    Value: !Sub "https://${CloudVaultApi}.execute-api.${AWS::Region}.amazonaws.com/"

  UserPoolId:
    Description: "Cognito User Pool ID"
    Value: !Ref CloudVaultUserPool

  UserPoolClientId:
    Description: "Cognito User Pool Client ID"
    Value: !Ref CloudVaultUserPoolClient
